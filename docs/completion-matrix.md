# Completion matrix

This file contains only known unfinished implementation, test, and acceptance work. Product
decisions belong in [product-spec.md](product-spec.md); a resolved decision row is removed
after its decision is recorded in normative documentation and any remaining implementation is
represented by an actionable row. A finished item is removed only after its acceptance
evidence exists. Muzilla is fully ready only when this file has no actionable rows and every
gate in
[production-readiness.md](production-readiness.md) passes on the candidate revision.

Risk uses S1 for core correctness or a potentially unsafe boundary, S2 for an important
workflow gap, and S3 for usability, maintainability, or additional assurance.

| ID | Type | Current state and why unfinished | Expected final behavior and acceptance | Relevant areas | Dependencies | Risk |
| --- | --- | --- | --- | --- | --- | ---: |
| UX-CATALOG-002 | UX | Facets are flat dropdowns capped at 500 values; only part of the facet model is exposed, and Catalog state is not fully URL-owned. | Search-first high-cardinality facet controls expose the supported combinable facets, active removable chips, keyboard behavior, counts/state consistent with server filtering, and restorable URL state. Cover large cardinality and pagination in API/frontend/E2E tests. | Catalog/facet hooks, catalog repo/service/API | UX-CATALOG-001 | S2 |
| UX-DASHBOARD-001 | UX | Missing/error counts link to filtered Catalog views, but album, singleton, ungrouped, and missing-art metrics remain non-actionable counts. | Every actionable health metric opens the corresponding filtered list or detail with an honest empty state; non-actionable facts are visually distinct from links. Verify navigation and filter semantics. | Dashboard, Catalog filters, dashboard API/tests | UX-CATALOG-002 | S3 |
| UX-IMPORT-001 | product/UX | The web import starts only at the configured root and does not preview selectable file/directory scope, exclusions, or active enrichment/rename policy. | Provide contained server-side file/directory scope selection, resolved scope/count and unsupported-file feedback, effective policy/cost preview, and outcome-based progress/cancel semantics. No client-filesystem fiction and no pre-Apply file writes. Cover a scoped import and cancellation in E2E. | Import UI/API/services, path guard, scan/import tests | SETTINGS-POLICY-001; COMPAT-CHANGESET-001 | S2 |
| UX-ACTIVITY-001 | product/UX | Activity is still primarily a raw job list and renders technical job type names; user sessions and Apply/Undo outcomes are not the primary aggregation. | Activity groups work by user action (scan/import, Apply, Undo, duplicate analysis), shows outcome/progress and supported cancellation, and moves job/event/attempt/correlation details behind diagnostics. System jobs stay hidden by default. | Jobs API/service, ImportSession, ApplyRun, ReviewUndoRun, `frontend/src/pages/Jobs.tsx` | COMPAT-CHANGESET-001 | S2 |
| UX-REVIEW-BULK-001 | UX/safety | A single review can be rejected reversibly, but there is no explicit multi-selection bulk reject or undo toast. | Bulk reject affects only visibly selected reviews, previews/counts its scope, persists decisions without writing files, and provides a working undo affordance. It must never target all rows in a filter implicitly. Test partial request failure and restored selection/list state. | Review inbox/API/service/frontend | — | S2 |
| UX-REVIEW-EDGE-001 | UX | Filtered neighbor navigation is tested beyond page one, but the item-removed-by-decision case remains uncovered; unsaved typed edit state can be abandoned without the specified discard boundary. Inbox filter controls expose only part of the server contract. | Implement the settled autosave-anchor contract: when autosave makes the currently open ReviewBundle leave the active filter, keep it open and anchored until the user leaves it, removing it from the filtered results only afterward. Block navigation, close, or filter change while typed edits are unsaved; the user must explicitly choose Restare (stay) or Scartare (discard), and edits are never implicitly saved. Expose the intended confidence/issue/source/session filters without losing URL state. Add focused frontend and E2E acceptance. | Review inbox/detail, neighbors API, typed editors, router tests | — | S2 |
| DOMAIN-DUPLICATES-001 | domain/UX | Duplicate groups store a recording ID and raw file facts but no normalized confidence, evidence, duration comparison, or quality assessment; the UI implies “verified” more strongly than the model supports. | Introduce one serialized evidence model with calibrated confidence and explained quality facts, distinguish false-positive/uncertain cases, and render it without automatic keep/delete decisions. Cover positive/negative corpus, migration, API, and UI. | duplicate models/pipeline/service/API/Catalog | — | S2 |
| DOMAIN-GROUPING-001 | architecture/cleanup | `TrackGroup`, legacy grouping services, comments, and compatibility concepts still encode the old user-facing collection model even though grouping is hidden from primary UX. | Complete the code/schema/client/test cleanup around internal inference/work-unit terminology without recreating public CRUD or arbitrary reassignment. Preserve constrained ReviewBundle grouping correction and test clean install/upgrades if persisted names change. | group models/pipeline/services/migrations/tests | COMPAT-CHANGESET-001 | S3 |
| OPS-RETENTION-001 | product/ops | The retention engine runs and its system job is hidden by default, but Settings does not expose effective age/count policy or explain the actual ReviewBundle undo horizon. | Settings shows effective journal age/count limits, validates supported edits, applies them at runtime or labels bootstrap-only values, and explains which condition expires Undo. Verify persistence/restart, expiry UI, and redaction/reset behavior. | config, settings API/service/UI, retention pipeline | COMPAT-CHANGESET-001 | S2 |
| SETTINGS-POLICY-001 | product/UX | Automatic metadata/art/lyrics/ReplayGain flags and filename collision policy exist in configuration but are not exposed as effective Settings controls. | Settings exposes effective automatic-enrichment and filename/collision behavior; every enabled control takes effect for new work, persists as intended, and reports capability constraints. Import/review display the policy actually used. | config, settings resolver/API/UI, match/enrichment/path handlers | — | S2 |
| SETTINGS-MATCHING-001 | product/UX | The Settings UI advertises fixed internal matching weights, while the product contract requires validated Advanced matching controls and provider ordering. | Settings → Advanced matching exposes principal signal weights, strong/ambiguous/reject thresholds, minimum first/second-candidate gap, and provider preference order with safe defaults, non-negative/semantically consistent validation, reasonable ranges, Reset to defaults, persistence/restart coverage, and raw scores in review. Provider order is a tie-breaker only and cannot override a materially better candidate; the equivalence zone derives from the matching contract and the effective configuration. | matching weights/thresholds, provider ranking, settings API/UI, review candidate explanations | — | S2 |
| SETTINGS-SECRETS-001 | product/ops | Provider credentials are stored and resolved only through the Muzilla-managed secret store; externally supplied secrets (Docker secrets, environment, external configuration) have no precedence contract and the UI cannot indicate an externally managed credential. | When an external secret exists for a provider it takes precedence over any UI-managed value; the UI marks that credential as externally managed/configured, does not overwrite it accidentally, and never consults a UI-managed value while an external one is active. UI-managed credentials persist across recreate/upgrade, are removed by factory reset, and remain the fallback when no external source exists. Cover precedence resolution, UI state, reset behavior, and secret redaction in API/UI tests. | config resolver, provider credential loading, Settings API/UI, reset | — | S2 |
| MATCHING-DECISION-001 | safety/product | The engine still exposes the legacy `auto_applicable` / `AUTO_THRESHOLD` / `needs_confirmation` decision bands, the CLI prints “auto-applicable”, and the settled strong/ambiguous/reject/skip contract is not enforced end-to-end: rejected candidates and explicit Skip are not modeled as user-visible resolution, and an unresolved ambiguous item does not yet block whole-bundle Apply. | Replace the `auto_applicable`/`AUTO_THRESHOLD` naming and user-facing copy with the settled strong/ambiguous/reject model. Strong matches only preselect a candidate that stays editable and is applied solely through explicit web UI Apply. Ambiguous matches show ordered candidates and require an explicit selection or Skip / Leave unchanged; an unresolved ambiguous item blocks Apply of the entire ReviewBundle. Rejected candidates never appear in choices and cannot be selected, force-matched, or applied; if no candidate clears the reject threshold the file stays unmatched/needs attention and is resolved only by explicit Skip. An explicit Skip / Leave unchanged resolves the item, is visibly distinct from unresolved, and never blocks the rest of the bundle. Cover every band across engine, pipeline, API, UI, and E2E tests. | matching engine/weights, pipeline proposals, ReviewBundle decision API/UI, CLI match copy | — | S1 |
| MATCHING-PROVENANCE-001 | matching/review | Candidate provenance and coherent primary releases exist in parts of the pipeline, but secondary enrichment boundaries and confirmation before replacing manual review values are not fully enforced or covered. | Keep one coherent primary release/recording identity, allow only explicitly supported complementary enrichment, preserve and display provenance, and require confirmation before a provider refresh replaces a user-manual value. Add regression/API/UI coverage for strong, ambiguous, rejected, and refreshed reviews. | matching engine/service, proposal revisions, provider enrichment, ReviewBundle UI/API | — | S1 |
| PIPELINE-CACHE-001 | engineering | The normalized semantic `provider_cache` model and helper primitives exist, but production retrieval/hydration paths do not use them, so offline replay and stale fallback are incomplete. | Mandatory persistent provider cache: route provider gateway retrieval through versioned cache entries with durable persistence, TTL, manual refresh, reuse of already-retrieved metadata, and automatic fallback to stale cached values when a provider is unavailable. Stale/offline state is explicit and visible with provenance; cached remote artwork needed by existing reviews is retained for review/apply continuity; offline mode must not discover metadata never retrieved; cache reset may remove cached metadata and artwork. Cover restart, offline, and provider-failure behavior in gateway/API/UI tests. | `providers/cache.py`, matching/manual-search/enrichment gateways, retention, review assets | — | S2 |
| PATH-COLLISION-001 | product/safety | Collisions are detected and blocked, but `%aunique`/`%sunique` render-time disambiguation still exists in the path engine and must be removed; the no-automatic-disambiguation policy is settled, not an open question. | No automatic disambiguation of any kind: never invent destination names or silently add suffixes, hashes, counters, or `%aunique`/`%sunique` values. Detect and show every conflict before Apply, block the entire ReviewBundle, require the user to change metadata or path configuration, and recalculate the preview before Apply is allowed again. Cover no-clobber, case-sensitive/insensitive, Unicode-normalization, invalid/long-name, multi-artist, multidisc, and concurrent-race behavior. | paths engine/functions, proposal composer, Settings, ReviewBundle/apply tests | SETTINGS-POLICY-001 | S1 |
| ART-COVER-SOURCE-001 | safety/compatibility | Remote embedded-art proposals exist and scanning ignores non-audio sidecars, but local cover upload behavior and complete remote-only source/replace/undo coverage remain. | Use only artwork retrieved from supported remote sources; ignore `cover.jpg`, `folder.jpg`, `album.jpg`, and equivalent sidecars as input/output and never read, create, modify, delete, or rename them. Allow a reliably associated non-primary art provider, user selection in review, old/new preview and journaled undo for replacement, cached-art continuity, and valid metadata Apply without artwork. Remove or migrate local-upload behavior and cover tests accordingly. | scan, cover assets/providers, ReviewBundle art operations/UI, apply/journal/undo/reset tests | COMPAT-CHANGESET-001 | S1 |
| REVIEW-CONFLICTS-001 | safety/concurrency | Source snapshots can detect some per-file drift, but drift does not yet block the entire ReviewBundle and concurrent reviews/jobs have no complete same-file conflict contract. | If any source file changes after preview, block Apply for the entire ReviewBundle and require refresh/re-review. Concurrent jobs/reviews targeting the same file produce an explicit conflict rather than last-writer-wins; destination races remain no-clobber conflicts. Cover locks/leases, stale snapshots, refresh, retry, and deterministic API/UI outcomes. | source snapshots, apply coordination, paths, ReviewBundle services/API/UI | REVIEW-ATOMICITY-001 | S1 |
| TEST-RESCAN-001 | acceptance | Pipeline and API tests cover single-track reread, but the real `rescan_track` handler with disposable audio and the complete “Analyze again” ordering are not covered. | Add handler/integration and browser acceptance proving reread updates the catalog, missing-race semantics are visible, providers are not contacted by reread, and analysis/matching starts only after successful reread. | scan handler, track detail, jobs/API tests | — | S2 |
| TEST-MATCHING-001 | quality/eval | The matching corpus has 17 scenarios and does not yet characterize false positives/negatives across the representative album/singleton workload. | A versioned corpus of at least 50 named scenarios in `tests/fixtures/matching` spans the representative album/singleton workload (clean, dirty, incomplete, near-duplicate, multi-disc, compilation, multi-artist, Unicode/case edges, and ambiguous cases) and pins each scenario's expected decision band (strong/ambiguous/reject), raw-score ordering, explanation content, and provider hydration. Fixed acceptance bands for false positives and negatives live in the corpus manifest; no live provider dependency and no dependency on a product-decision row. | matching fixtures/engine/service tests | — | S2 |
| TEST-FORMATS-001 | compatibility | Round-trip fixtures cover MP3, FLAC, Ogg/Vorbis, Opus, M4A, WAV, and AIFF; WavPack, WMA/ASF, and DSF read/write/apply/undo evidence is incomplete. | Classify each format as supported, best-effort, or unsupported from implementation and disposable-fixture evidence. Add read/write/Apply/Undo verification for formats claimed supported, or explicitly reject and document formats without that evidence. Never infer support from an extension or silently accept an unverified reviewed write. | tags, scanner, apply/undo fixtures, support documentation | — | S2 |
| TEST-PATH-COMPAT-001 | compatibility | The path engine calls its syntax beets-compatible, but no imported compatibility corpus verifies that claim or documents intentional divergences. | Run a maintained compatibility corpus for the supported beets-style syntax, document deliberate differences, and ensure unsupported constructs fail explicitly. Fuzz/property tests remain complementary, not a substitute. | `paths/`, path tests, configuration docs | PATH-COLLISION-001 | S3 |
| TEST-NATIVE-001 | runtime/acceptance | The exact image executes `rsgain`, but no exact-image functional check exercises `fpcalc` against audio; host tests may skip when the binary is absent. | Candidate-image verification runs `fpcalc` functionally as the non-root runtime user against disposable audio and confirms capability/error behavior through the supported Muzilla API/worker path, not merely binary presence or help output. Keep native checks deterministic and isolated. | Dockerfile, container tests, fingerprint capability/handler | — | S1 |
| TEST-UX-MANUAL-001 | accessibility/responsive acceptance | Automated shell/review tests cover major routes, but the full 200% zoom and low-height/landscape/safe-area/long-text/font-scaling/manual keyboard matrix is not closed with current evidence. | Execute and record repeatable acceptance (automated where stable, explicit checklist where inherently visual) for primary journeys at desktop/tablet/mobile, 200% zoom, low viewport, long text, focus return, reduced motion, and no-hover use. Fix any observed blocker before closure. | AppShell, Catalog, Reviews, dialogs, E2E | UX-CATALOG-001; UX-CATALOG-002; UX-REVIEW-EDGE-001 | S2 |
| PERF-SCALE-001 | production readiness | The final architecture has not been revalidated end-to-end at the declared upper scale in the exact candidate image. | On the exact final candidate image, measure at least 100,000 representative tracks including albums, singles, duplicates/near-duplicates, incomplete/inconsistent metadata, multidisc/compilation/multi-artist cases, Unicode/case edges, embedded art, and ambiguous matches. Use realistically sized audio for relevant I/O paths, SSD baseline, record benchmark hardware, and target 2 GiB Muzilla-owned RAM. Cover cold/incremental scan, Catalog/search/filters/facets, grouping, matching, ReviewBundle generation/navigation, Apply, Undo, cancellation, responsiveness, throughput, memory, and resources. Define quantitative thresholds before the run, do not require a hardware-independent total runtime, and do not relax thresholds after the run; any target above 2 GiB requires evidence and justification. | perf scripts, Docker/Compose, DB queries, Catalog/review APIs | — | S2 |
