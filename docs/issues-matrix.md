# Recovery issues matrix

Ultimo aggiornamento: 2026-08-08. Questa matrice è il registro operativo dell'audit di
recovery. Le diagnosi derivano dal codice a `346c919`, dai test locali e dall'ispezione
dell'immagine `muzilla:local`; le righe risolte documentano le correzioni implementate.

## Legenda

- **Severità:** S0 perdita di dati/security critica; S1 flusso core bloccato o risultato
  materialmente errato; S2 funzione importante degradata; S3 usabilità/manutenibilità;
  S4 cosmetico.
- **Stato:** `confermato` = riprodotto o dimostrato nel percorso di codice; `parziale` =
  sintomo verificato ma una parte richiede un test mirato; `gap` = funzionalità richiesta
  assente; `decisione` = problema di prodotto/UX, non bug locale; `riservato` = ID usato
  solo come esempio nel brief.
- Le righe non risolte indicano i test da aggiungere o correggere; le righe risolte
  riportano le verifiche effettivamente eseguite.

## Elementi richiesti

| ID | Categoria | Descrizione / root cause | Sev. | Sottosistema | Decisione proposta | Test necessario | Dipendenze | Stato |
|---|---|---|---:|---|---|---|---|---|
| BUG-JOBS-001 | bug | Risolto: `CancellationToken` effettua letture DB throttled da sessione indipendente, mai dall'istanza ORM leased. Pending passa subito a `cancelled`; running passa a `cancelling` fino al checkpoint. Scan persiste gli indici già letti e si ferma per file; import propaga il token e marca session/task non completati `cancelled`; enrichment controlla prima/dopo fetch o compute e scarta una proposal in-flight. Nessun checkpoint interrompe il commit atomico dell'apply. | S1 | jobs, scan, import, UI | Conservati lease, `JobEvent` e worker in-process; il result di un job cancellato espone l'outcome parziale. | `tests/jobs/test_queue.py`, `tests/jobs/test_worker.py` (scan reale e lyrics reale con cancel da altra sessione), handler import/enrichment; `e2e/tests/jobs.spec.ts` (Cancel UI su scan reale, terminale solo `cancelled`). | Modello stato job; pipeline | risolto |
| BUG-REPLAYGAIN-001 | bug/ops | L'immagine conteneva `/usr/local/bin/rsgain`, ma il purge dei build deps rimuoveva `libtag.so.2`, `libebur128.so.1`, `libinih.so.1`, `libfmt.so.10`; il health check verificava solo HTTP. | S1 | Docker, audio, health | Risolto con builder separato, suite Debian pin, runtime packages espliciti, assertion finale `ldd`/`rsgain --version`, liveness `/api/health`, readiness `/api/ready`, capability `/api/capabilities`, probe HTTP single-flight e preflight API/CLI/worker che impedisce successi falsi dopo restart o failure totale. | `tests/container/test_runtime.py`, `test_build_guard.py`, `compose_smoke.py`; `tests/services/test_capabilities.py`; `tests/api/test_health.py`, `test_enrichment.py`; `tests/jobs/handlers/test_enrich_replaygain_handler.py`; `tests/cli/test_enrich.py`; `frontend/src/pages/Jobs.test.tsx`; smoke immagine/Compose. | Prima slice; nessuna migrazione | risolto |
| BUG-LYRICS-001 | bug | Risolto: LRCLIB restituisce assenza per 404, classifica 408/429/5xx e timeout come transient e gli altri 4xx come permanent. Retry limitato (3 tentativi), exponential backoff con full jitter e rispetto di `Retry-After`; nessuna chiamata live nei test. L'enrichment persiste outcome per track (`found`, `not_found`, `transient_error`, `permanent_error`) e i soli transient sono selezionabili da `Retry failed`. | S2 | provider LRCLIB, jobs | La review/changeset già creata resta intatta su failure parziale; `not_found` non è ritentato, mentre il retry crea un job ristretto ai soli `track_ids` transient. | `tests/providers/test_lrclib.py` (408/429/5xx/timeout/budget), `tests/jobs/handlers/test_enrich_lyrics_handler.py`, `tests/api/test_enrichment.py`; UI Jobs/endpoint retry. | Contratto provider; ReviewBundle | risolto |
| BUG-REVIEW-001 | bug | Risolto nella Slice 4 Terra: `Operation` espone una union OpenAPI discriminata per `kind`; `WriteLyrics` richiede e restituisce `{text, synced, provider}`. Anche l'editor ChangeSet legacy ora estrae/modifica solo `text` e conserva `synced`/`provider`, quindi un payload non può diventare `[object Object]`. | S1 | review frontend, API contract | Nessun producer è stato dual-written. La route ReviewBundle è read-only e il nuovo UI completo resta fuori scope; l'editor legacy mantiene la compatibilità fino alla migrazione one-way del producer. | `tests/services/test_reviews.py`, `tests/api/test_reviews.py`, `frontend/src/lib/lyrics.test.ts`; typecheck/frontend build. | CONTRACT-API-001 | risolto |
| BUG-REVIEW-002 | bug | Risolto nella Slice 5: `ReleaseCandidate.track_count` è dichiarato dal summary o dall'item idratato e resta `null` quando il provider non lo conosce; non è più derivato da una tracklist assente. Il gateway idrata la shortlist prima del rank. | S2 | provider, matching, candidate UI | Il contract candidate espone count, tipo/identità/durata della traccia rappresentativa, cover e segnali di score. | `tests/services/test_matching.py`, `tests/api/test_matching.py` (summary→hydrate→rank, count); `tests/providers/test_deezer.py`. | BUG-MATCHING-001 | risolto |
| BUG-MATCHING-001 | bug | Risolto nella Slice 5: filename parser con confidence integra solo tag mancanti; retrieve è limitato, deduplica ref e idrata prima del rank. Le query provider usano campi track/release disponibili; score serializza segnali/penalità e rifiuta candidati privi di identità o sopra soglia assoluta. `token_set_ratio` non è più usato, quindi subset/ripetizioni non collassano. | S1 | matching, provider, pipeline | Pipeline `parse→retrieve→hydrate shortlist→rank→reject`, con failure provider distinto da zero results e nessun candidato presentato se rifiutato. | `tests/matching/test_filename.py`, `test_candidates.py`, `test_engine.py`, `test_scoring_corpus.py`, `tests/services/test_matching.py`; fixture Piki/repeat/parentesi/metadati mancanti. | Contratti candidate; FEATURE-SEARCH-* | risolto |
| BUG-FILE-001 | bug/prodotto | Risolto nella Slice 8 e nei finding REVIEW-S8-A1/A5: il manifest congelato applica tag e move nello stesso esito per file. Il writer chiude anche la race collision-check→rename con no-clobber atomico nativo, ammette il case-only dello stesso file e mantiene containment, symlink guard, fsync e journal. Dopo replace/move riallinea stat, content/tag hash e path del catalogo, quindi il rescan riusa la stessa riga senza `missing` o update spurio. | S1 | proposal, paths, apply | Conservati applier e `ApplyJournal`; l'aggregazione bundle dichiara solo atomicità per file e può risultare parziale. Piattaforme senza rename esclusivo nativo falliscono chiuse. | `tests/changes/test_apply_review_bundle.py` (collisione creata dopo precheck; stat/hash→rescan), `tests/changes/test_apply_move.py`, `tests/services/test_proposals.py`. | PRODUCT-FILENAME-001; ReviewBundle | risolto |
| BUG-CHANGES-001 | bug/modello | Foundation Sol mantiene identità/revisioni idempotenti. Terra aggiunge la lettura contrattuale e un adapter stretto da revisioni correnti accepted a un unico ChangeSet legacy; il matching legacy non è ancora migrato e quindi non esiste dual-write. | S1 | changesets, matching API | Migrazione one-way senza backfill: quando un producer verrà spostato userà ReviewBundle e l'adapter temporaneo per l'applier; i producer non spostati restano solo legacy. | `tests/services/test_reviews.py` (session/restart/concorrenza), `tests/api/test_reviews.py`, `tests/changes/test_review_adapter.py`. | DOMAIN-CHANGES-001 | parziale (producer legacy resta) |
| BUG-DASHBOARD-001 | bug/UX | Slice 8 risolve la parte di consistenza: apply move→catalog→rescan aggiorna la stessa Track e non genera `missing` spurio. Resta il finding UX: la dashboard espone solo il conteggio e catalogo non offre filtro/detail delle anomalie. | S2 | dashboard, catalog, scan | La Slice 10 deve rendere le metriche drill-down con file/path/azione; nessuna compensazione dashboard è necessaria per i move applicati. | `tests/changes/test_apply_review_bundle.py::test_apply_move_catalog_rescan_does_not_mark_track_missing`; missing reale→dashboard→detail resta Slice 10. | UX-CATALOG-001 | parziale (consistenza risolta; drill-down resta) |
| BUG-NAV-001 | bug/UX | La selezione candidato naviga con push a un nuovo changeset; Back riapre il draft precedente. Breadcrumb e origine sono hard-coded e filtri/scroll non sono URL-state. | S2 | router, review | Review stabile + `returnTo`; replace quando cambia revisione; prev/next e stato lista nell'URL. | Browser Back/Close da catalogo, inbox e import; filtri/scroll. | BUG-CHANGES-001; UX-CHANGES-005 | confermato |
| BUG-SHORTCUTS-001 | bug | J/K aggiornano solo l'indice di focus; nessun ref o `scrollIntoView`. | S3 | review frontend | Fix locale accessibile: roving focus, scroll nearest, rispetto input/modal e reduced motion. | Keyboard + viewport assertion. | Nuovo layout review | confermato |
| OPS-PROVIDERS-001 | affidabilità | Risolto nella Slice 3 Terra: client live e config sono pubblicati come uno snapshot atomico a lease; startup/reload lancia probe bounded senza bloccare readiness. I probe portano la generazione dello snapshot, quindi un risultato vecchio non può sovrascrivere lo stato del reload successivo; un timeout diventa stato esplicito, non 500. Nessun client in uso viene chiuso durante il request/job che lo ha acquisito. | S2 | provider health, dashboard | Stato esplicito: `disabled`, `not_configured`, `checking`, `operational`, `temporary_unavailable`, `invalid_credentials`; `last_checked_at` e diagnostica sanificata. Un 404 upstream conferma reachability e non è un outage. | `tests/providers/test_runtime.py`, `tests/providers/test_status.py`, `tests/services/test_providers.py`, `tests/api/test_providers.py`; startup/restart e test connection. | OPS-SETTINGS-001 | risolto |
| OPS-SECRETS-001 | security/ops | Nucleo Sol implementato: i nuovi token sono file opachi atomici nello store configurabile `storage.provider_secrets_dir` (default accanto al DB sotto `secrets/providers`); SQLite contiene solo un `secret_ref` versionato. La scrittura del file precede il commit che pubblica il nuovo riferimento: un commit fallito conserva il secret precedente e ripulisce il candidato quando lo store resta disponibile. Startup migra il legacy con ordine secret durabile+fsync → `secure_delete`/commit → checkpoint WAL `TRUNCATE`; un checkpoint busy/non completo interrompe startup ed è ritentato, anche quando la riga è già migrata. Restart/recreate sullo stesso `/data` preservano store e riferimenti. | S1 | settings, deployment | Threat model: directory/file `0700`/`0600`, owner corrente, reference allow-listed/hash, rifiuto di symlink, file non regolari, owner/permessi deboli; API, log e normale export SQLite post-migrazione non contengono token. Un crash o un failure nella rimozione può lasciare un file owner-only non referenziato, ma non cambia quale secret è effettivo né trasforma un commit riuscito in un falso errore. Non protegge da root/compromissione dello stesso UID, copie/backup storici creati prima della migrazione o cancellazione del volume; un backup completo del volume include intenzionalmente lo store. Factory reset/backup UX resta a `SETTINGS-RESET-001`. | `tests/services/test_secrets.py` (permission, symlink, reference negative); `tests/services/test_settings.py` (write/clear, commit failure, replace/retired cleanup, migrazione retryable e WAL busy); `tests/api/test_settings.py` (API/log/DB+WAL negative, startup+reinstall). Nessuna migrazione Alembic: data migration startup idempotente prima di client/worker. | OPS-SETTINGS-001; SETTINGS-RESET-001 | parziale (secret store e migrazione risolti; reset resta) |
| OPS-PROVIDERS-002 | UX/ops | Risolto nella Slice 3 Terra: Dashboard e Settings mostrano badge testuali senza `unknown`; Settings espone configurazione token write-only, stato, ultimo controllo/errore, “Test connection” e “Clear credential” con conferma. | S2 | settings, provider UI | Schede provider con auth richiesta/opzionale, stato esplicito, salvataggio/sostituzione/rimozione della credenziale, test connection e ultimo errore sanificato. | `frontend/src/components/ProviderHealthPanel.test.tsx`, `frontend/src/pages/Settings.test.tsx`, typecheck. | OPS-PROVIDERS-001; OPS-SECRETS-001 | risolto |
| UI-GROUPS-001 | UI | Riga flex a altezza fissa senza overflow/wrap/min-width coerenti; colonne e azioni possono sovrapporsi. | S3 | Groups UI | Non investire nella tabella attuale: rimuovere la pagina primaria; nel resolver residuo usare layout responsivo. | Viewport e contenuti lunghi. | DOMAIN-GROUPS-001 | confermato |
| UI-SIDEBAR-001 | UI | Shell `h-screen` più pagine `min-h-screen/h-screen`; sidebar senza proprio overflow. Il footer/sign-out compete con contenuti lunghi. | S2 | AppShell | Una sola shell `100dvh`, nav scrollabile e footer stabile, content outlet `min-h-0 overflow-auto`, drawer mobile. | Desktop/mobile, zoom 200%, viewport corto. | Redesign shell | confermato |
| UI-SHORTCUTS-001 | UI | Footer scorciatoie `fixed` non riserva spazio, attraversa anche la sidebar e ignora safe-area/mobile. | S3 | review | Toolbar sticky nel pannello o bottom inset riservato; collassabile e nascosta su touch quando inutile. | No-overlap, safe area, zoom/mobile. | UI-SIDEBAR-001 | confermato |
| UI-COVERS-001 | UI | Le summary di changes non includono cover corrente/candidata; la review mostra blob solo per change art già creati. | S3 | API summary, lists | Thumbnail lazy/cached con placeholder e testo alternativo; dati cover nella ReviewSummary. | Blob unavailable, lazy load, a11y, lista grande. | PRODUCT-REVIEW-001; ReviewBundle | confermato |
| UI-CATALOG-001 | UI | Sorting esiste solo in dropdown, non per header; colonne flex fisse, testo ellittico, nessun resize o modalità leggibile. | S2 | Catalog UI | Mantenere data-grid virtualizzata: header sorting, width prefs, overflow controllato e dettaglio rapido; card list su mobile. | Keyboard sort/resize, persistence, long text, mobile. | UX-CATALOG-* | parziale |
| UI-ICONS-001 | UI/a11y | Dot/icon hanno aria-label/title, ma per utenti vedenti lo stato essenziale resta dipendente da simbolo/hover. | S3 | Catalog UI | Colonna “Stato” con badge testuali e filtri; icona solo ridondante. | Screen reader, no hover, contrast/non-color. | UI-CATALOG-001 | confermato |
| UI-CANDIDATES-001 | UI | Risolto il contract nella Slice 5: il candidate include tipo, track count dichiarato/null, traccia rappresentativa (titolo/artista/posizione/durata), cover, duplicate provenance e segnali/penalità di score; gli esiti provider sono separati. La CandidateCard visuale resta nella slice UI. | S2 | matching API, review | CandidateCard gerarchica, summary compatta + confronto espandibile; evitare densità indiscriminata. | `tests/api/test_matching.py`, OpenAPI rigenerato; card visuale nella Slice 9. | BUG-REVIEW-002; BUG-MATCHING-001 | parziale (contract risolto; UI differita) |
| UX-GROUPS-001 | IA | `TrackGroup` è un cluster inferito utile alla pipeline, ma la UI espone il nome tecnico e operazioni interne senza spiegazione. | S2 | dominio, IA | Conservare il cluster interno; eliminare `/groups` dalla nav e integrare i casi incerti in “Risolvi raccolta” dentro review/import. | Journey di correzione grouping a bassa confidenza. | DOMAIN-GROUPS-001 | decisione |
| UX-GROUPS-002 | UX/rischio | Il reassign consente spostamento verso qualunque gruppo, anche artista/album incompatibile, e viene applicato subito. | S1 | grouping service/UI | Rimuovere l'azione generale; consentire solo correzioni vincolate con preview/review, override avanzato esplicito se davvero necessario. | Incompatibilità bloccata; nessuna apply immediata. | PRODUCT-GROUPS-001 | confermato |
| UX-TERMINOLOGY-001 | UX | “Force to singleton” descrive l'implementazione, non l'esito. | S3 | grouping UI | Nel resolver usare “Tratta come brano singolo” + conseguenza; rimuovere dalla nav primaria. | Copy/accessibility. | UX-GROUPS-001 | decisione |
| UX-CHANGES-001 | UX | Lista minimale; la riga non è l'azione primaria, dettaglio/filtri/selezione/confidenza/errori assenti. | S2 | ChangesList | Sostituire con Inbox revisioni: intera riga apribile, preview, filtri, ricerca, bulk decision sicura e next-unreviewed. | Inbox desktop/mobile e keyboard. | DOMAIN-CHANGES-001 | confermato |
| UX-REVIEW-001 | UX | Review centrata su righe tecniche di `Change`, non sul file/candidato/esito; enrichment è frammentato in changeset separati. | S1 | review, pipeline | Detail di ReviewBundle file-first, sezioni tag/file/cover/lyrics/audio, uncertainty ed errori per task. | Journey unico e fallimenti parziali. | PRODUCT-PIPELINE-001 | decisione |
| UX-REVIEW-002 | UX | Nessun prev/next/next-unreviewed; ritorno obbligato alla lista. | S2 | router, inbox | Navigazione contestuale basata sull'ordine filtrato e `returnTo`; autosave decisioni. | Bordi lista, item rimosso dal filtro, refresh. | BUG-NAV-001 | gap |
| UX-CHANGES-002 | UX | Summary non espone filename/path/metadati originali/scan; il titolo del changeset è insufficiente. | S2 | summary API, inbox | Introdurre SourceFileSummary immutabile/snapshot nella review. | File missing/moved; multi-track release. | Modello ReviewBundle | confermato |
| UX-CATALOG-001 | UX | Click apre editor tag, non offre matching, refresh provider o rescan; le azioni sono disperse. | S2 | Catalog/detail | Track detail/action sheet: Review metadata, ricerca candidati, modifica manuale, rescan file. | Tutti i percorsi e ritorno al catalogo. | PRODUCT-SCAN-001 | parziale |
| UX-CATALOG-002 | UX | Facet server-side fino a 500 valori in dropdown; tecnicamente corretti ma poco scalabili/usabili. | S3 | Catalog | Filtri combinabili ricercabili e chip attivi; default search-first; rimuovere dropdown piatti dopo telemetry/test di usabilità. | Cardinalità alta, keyboard, URL state. | UI-CATALOG-001 | decisione |
| UX-CHANGES-003 | UX | Endpoint/lista changeset non supportano ricerca libera né campi origine indicizzati. | S2 | inbox, persistence | Query indicizzata su snapshot file, metadata, provider, stato, errore e ID. | Search syntax literal, pagination, combined filters. | Modello ReviewBundle; BUG-CATALOG-001 | gap |
| UX-CHANGES-004 | UX | Nessun reject-all rapido a livello review/selezione/risultato filtrato. | S2 | inbox/review | “Rifiuta proposte” per review reversibile; bulk su selezione esplicita con preview/count, mai implicitamente su tutto il filtro. | Undo, mixed state, confirmation bulk. | Lifecycle review | gap |
| UX-CHANGES-005 | UX | Back/close/cancel/save draft non hanno contratto comune; il breadcrumb forza `/changes`. | S2 | router/state | Definire: Back=history reale, Close=`returnTo`, Save=autosave, Discard=azione esplicita, prev/next non alterano history inutilmente. | Matrice navigazione per origine. | BUG-NAV-001 | confermato |
| PRODUCT-PIPELINE-001 | prodotto/architettura | Slice 7/7A compone una ReviewBundle stabile con job tecnici distinti. Slice 8 e REVIEW-S8-A2/A3/A6 congelano manifest e SourceSnapshot, applicano ogni file indipendentemente e persistono esiti per file. Un retry conserva la precondition congelata finché non è provato un side effect riuscito/recuperato; recovery distingue `done`, `reverted` e `failed` senza dichiarare revert non riusciti; Cancel viene osservato soltanto tra file e lascia gli attempt non iniziati retryable. | S1 | pipeline, data model | Nessuna transazione globale: job tecnici e ChangeSet per file restano separati sotto un'unica ReviewBundle; un bundle misto è `partially_applied`. | `tests/changes/test_apply_review_bundle.py` (retry senza side effect e crash recovery), `tests/changes/test_apply_journal_recovery.py`, `tests/jobs/handlers/test_apply_handler.py` (cancel dopo il primo file), `tests/services/test_review_apply.py`, `tests/api/test_reviews.py`; resta l'E2E della review unificata. | DOMAIN-CHANGES-001; matching | parziale (core backend completo; UI/E2E resta) |
| PRODUCT-PIPELINE-002 | prodotto | Slice 7/7C configura default automatici per metadata/cover/lyrics/RG; `metadata_auto=false` interrompe soltanto il producer automatico di matching, mentre ReviewBundle e ricerca/import manuale restano disponibili. I job rete mantengono la coda normale, RG usa priorità CPU bassa; rate-limit/cache restano nei rispettivi adapter. | S2 | pipeline/jobs/settings | Default: metadata+cover+lyrics automatici in background; ReplayGain automatico ma coda CPU a bassa priorità; tutti configurabili e non bloccanti. | `tests/jobs/handlers/test_match_handler.py` (opt-out metadata e TaskAttempt), handler cover/lyrics/RG ReviewBundle (successo, failure parziale, cancel/retry), OpenAPI codegen e frontend typecheck; offline/cache e E2E restano. | PRODUCT-PIPELINE-001 | parziale |
| PRODUCT-REVIEW-001 | prodotto/security | Slice 7/7B rende la cover un’operation tipizzata e non mutante nella ReviewBundle, con candidate scoped e upload validato. Slice 8 e REVIEW-S8-A4 completano apply/undo tramite journal e BlobStore/refcount: art, lyrics, tag e ReplayGain sono scritti insieme sul temporaneo; per le lyrics l'undo cerca il journal `tags` completato, non il journal più recente che dopo un move non contiene il payload precedente, e ripristina così il testo preesistente invece di cancellarlo. | S2 | art provider, review, blob security | Conservati BlobStore, writer e adapter ChangeSet per file; il journal è la fonte del contenuto lyrics precedente non cacheato nel catalogo. | `tests/changes/test_apply_review_bundle.py::test_bundle_apply_undo_restores_preexisting_lyrics_tag_and_path`, `tests/changes/test_apply_write_lyrics.py::test_undo_of_write_lyrics_restores_preexisting_text`, `tests/api/test_reviews.py`, `tests/audio/test_art.py`, `tests/services/test_migrate.py`. | DOMAIN-ART-001; ReviewBundle | risolto |
| PRODUCT-FILENAME-001 | prodotto | Slice 7 rende `$artist - $title` il default comune e conserva collision/error preview nell’operation move. | S2 | path templates | Rendere `$artist - $title` default comune; per collisioni aggiungere disambiguatore deterministico in preview, configurazione avanzata opzionale. | `tests/services/test_proposals.py`; invalid chars, Unicode, multi-artist, collision/case/length, multidisc. | BUG-FILE-001 | parziale |
| PRODUCT-SCAN-001 | prodotto | Scan lavora sulla library intera; non esiste comando/API per rilettura/rescan di un Track. Refresh candidati non rilegge il file. | S2 | scan, track detail | Endpoint/job `rescan_track`: existence + tags + fingerprint invalidation; azioni separate per refresh candidate e nuova review. | File changed/missing/moved; concurrency con apply. | Track workflow | gap |
| FEATURE-SEARCH-001 | feature | Risolto nella Slice 6 Terra: la ricerca manuale dentro una ReviewBundle normalizza titolo/artista/album e anno/durata/ISRC opzionali, espone capability/selection provider, pagina risultati già hydrated/ranked con dedup provenance e conserva zero hit, non configurato e failure come stati distinti. L'import del candidato riusa l'adapter Matching v2 e `put_revision`, quindi aggiorna lo stesso bundle in modo idempotente senza creare una nuova history entry. | S2 | provider gateway, review, manual search UI | Nessun secondo scoring/cache/adapter: la query esplicita passa da retrieve→hydrate→rank esistente e restringe soltanto il ProviderSet leased. | `tests/api/test_matching.py` (normalizzazione, stati provider, import/history idempotente), `e2e/tests/manual_search.spec.ts`. | BUG-MATCHING-001 | risolto (scope Terra) |
| FEATURE-SEARCH-002 | feature/security | Risolto nella Slice 6 Sol e nella sub-slice alias A2: registry puro riconosce soltanto URL HTTPS con host/path allow-listed per release MusicBrainz, album/track Deezer e release Discogs; restituisce esclusivamente provider/tipo/ID. Il fetch usa i client API configurati e metodi by-ID, mai l'URL/host utente. Playlist Deezer, master Discogs, recording MusicBrainz, authority ambigue, controlli ASCII, credenziali/porte, host locali/IP/suffix e path encoded sono rifiutati prima del networking. L'identità URL provider/tipo/ID è conservata dalla migrazione additiva 0012 in `candidate_url_aliases`, associata alla revisione ma esclusa dal suo contenuto/digest: URL track con album già corrente non rifà il fetch e URL→import manuale dello stesso candidato non crea history. | S2 | provider URL registry, review API | Deezer aggiunge la capability track-by-ID e idrata l'album canonico; MusicBrainz/Discogs riusano `get_release`. Errori not-found, non configurato, auth e temporaneo restano distinti. | `tests/providers/test_url_registry.py`, `tests/providers/test_deezer.py::test_get_track_candidate_resolves_track_id_then_hydrates_its_album`, `tests/api/test_matching.py` (controlli/SSRF no-fetch, ID-only, alias persistente fuori contenuto/digest, concorrenza e URL↔manual senza nuova revisione), `tests/services/test_migrate.py` (0011→0012, unique/check, cascade FK, downgrade/re-upgrade), `e2e/tests/url_candidate.spec.ts` (MusicBrainz, Deezer album/track, Discogs). | FEATURE-SEARCH-001; security | risolto (scope Sol + sub-slice alias A2) |
| BUG-DEEZER-001 | bug | Risolto nella Slice 5: singleton/ISRC usa `/search/track` con query progressive massimo tre e dedup per album, mentre album conserva `/search/album`; l'hit track porta ID album, count dichiarato e traccia rappresentativa, poi viene idratato e classificato localmente. | S2 | Deezer adapter, matching | Strategie track/album distinte con budget, rank locale dopo hydrate. | `tests/providers/test_deezer.py`, corpus parentesi/repeat/Piki, `tests/services/test_matching.py`. | BUG-MATCHING-001 | risolto |
| DOMAIN-DUPLICATES-001 | dominio/UX | Detector principale raggruppa stesso MB recording ID derivato da AcoustID; un'analisi separata usa artist/title, creando due concetti. UI mostra solo `basis=acoustid` e dati file. | S2 | duplicates, analyze | Un solo Evidence model: fingerprint/recording ID, confidence, durata e qualità; spiegazione e nessuna cancellazione automatica. | Falsi positivi/negativi, evidence serialization/UI. | UI redesign | confermato |
| DOMAIN-GROUPS-001 | dominio | Gruppo = cluster inferito album/singleton/partial, sticky se pinned; è necessario alla pipeline ma non è un'entità mentale utente stabile. | S2 | grouping/data model | Rinominare internamente CollectionCluster/WorkUnit; non esporre CRUD generale. Migrare correzioni nel ReviewBundle. | Inference/pinning/migration/rescan. | UX-GROUPS-* | decisione |
| DOMAIN-CHANGES-001 | dominio | La foundation espone `ReviewBundle`, snapshot/revisioni immutabili, task e attempt separati. Slice 8 porta l'apply sul percorso one-way ReviewBundle → manifest per file → adapter ChangeSet → applier/ApplyJournal esistenti, con result e retry persistenti; la migrazione 0014 abilita il rientro controllato da `failed`/`partially_applied` ad `applying`. | S1 | changes/data model/API | Nessun dual-write e nessun backfill: il ChangeSet per file resta un adapter temporaneo del solo writer, mentre draft/history legacy sono preservati. | `tests/domain/test_reviews.py`, `tests/services/test_reviews.py`, `tests/services/test_review_apply.py`, `tests/services/test_migrate.py`, `tests/api/test_reviews.py`, `tests/changes/test_review_adapter.py`, `tests/changes/test_apply_review_bundle.py`. | È il fulcro del recovery | parziale (producer/UI legacy restano) |
| PRODUCT-BULK-EDIT-001 | prodotto | UI/API e test supportano bulk edit, find/replace e strip; non è richiesto dal nuovo journey e amplia molto review e selezione. | S3 | catalog/edit | Rimuovere le entry UI nel redesign; conservare temporaneamente composer interno solo se riusato da azioni review, poi eliminare endpoint/codice morto. | Verificare nessun consumer; migrazione route/bookmark. | Nuova inbox/catalog | decisione |
| PRODUCT-GROUPS-001 | prodotto/rischio | Reassign cross-group è una correzione tecnica esposta come feature e auto-applicata. | S1 | grouping | Eliminare dall'UI/API pubblica; sostituire con resolver vincolato e revisionato. | Contract/authorization; incompatible groups. | DOMAIN-GROUPS-001 | decisione |
| PRODUCT-RETENTION-001 | prodotto/ops | `retention_sweep` parte all'avvio/ogni 24 h, pota journal/blob/cache scaduti e marca `undo_expired`; appare come job utente. | S3 | retention, jobs UI | Nascondere system jobs di default; Settings mostra solo policy “Cronologia annullamento” ed eventuale esecuzione amministrativa spiegata. | Retention/refcounts/UI filtering/audit. | Lifecycle review | confermato |
| SETTINGS-RESET-001 | admin/security | Nessun reset UI/API. | S2 | settings, DB, worker | Due azioni: reset libreria preservando configurazione/segreti; factory reset completo. Quiesce worker, frase forte, origin/CSRF, audit, non tocca mai i file musicali. | Auth/CSRF, running jobs, referential cleanup, restart/migrations, filesystem untouched. | OPS-SECRETS-001; data model | gap |
| FEAT-MATCHING-001 | nomenclatura | L'ID compare nel brief solo come esempio di naming, non descrive un requisito distinto. | — | — | Riservato; le attività reali sono BUG-MATCHING-001 e FEATURE-SEARCH-*. | Nessuno. | — | riservato |

## Rilievi aggiuntivi dell'audit

| ID | Categoria | Descrizione / root cause | Sev. | Sottosistema | Decisione proposta | Test necessario | Dipendenze | Stato |
|---|---|---|---:|---|---|---|---|---|
| BUG-CATALOG-001 | bug | La query utente è passata direttamente a FTS5 `MATCH`; `-`, `"`, `:`, operatori e altre forme causano `sqlite3.OperationalError`/500. Un E2E oggi considera corretto mostrare l'errore. | S1 | catalog DB/API | Parser literal sicuro o query mode esplicito; input ordinario non deve mai diventare sintassi FTS. Correggere il test che consolida il 500. | Hyphen, quote, colon, operators, Unicode, empty; API non-500. | Search inbox riusa la soluzione | confermato |
| OPS-SETTINGS-001 | bug/ops | Risolto nella Slice 3 Terra: `EffectiveConfigResolver` fonde Config bootstrap, override DB non-secret e `secret_ref` letto soltanto nello store esterno; env `MUZILLA_PROVIDERS__…` resta dominante. Settings legge e restituisce la stessa configurazione effettiva pubblicata con i client, non la proiezione grezza dell'override DB. Save costruisce un set completo e lo pubblica con swap atomico; request e job acquisiscono lease, poi i client ritirati vengono chiusi. Restart/recreate ripete lo stesso resolver prima di worker/client. | S1 | startup, settings, providers | Nessun token passa a DTO/log/export/DB: il resolver crea solo `SecretStr` effimera. Il migration/threat model del secret store resta in OPS-SECRETS-001; non sono state introdotte migrazioni Alembic né dipendenze bootstrap dal DB. | `tests/services/test_settings.py` (default effettivi, resolver/env precedence), `tests/api/test_settings.py` (default e save→live), `tests/providers/test_runtime.py` (lease/close); negativi secret/API/DB/WAL in OPS-SECRETS-001. | OPS-PROVIDERS-*; OPS-SECRETS-001 | risolto |
| CONTRACT-API-001 | maintainability/bug risk | Risolto nella Slice 4 Terra: `lib/types.ts` è ora un layer di nomi/adapter sopra `api-types.ts` generato; `api.ts` usa quei tipi e i request model generati. `ProposalRevisionOut.operations` è una union discriminata `kind` con varianti esplicite, inclusa `WriteLyrics`. | S2 | frontend API | Gli adapter restano solo per concern di vista o endpoint legacy senza enum response/SSE; non duplicano più la forma server. CI continua generate-and-diff. | `tests/api/test_reviews.py` (OpenAPI discriminator), `npm run generate-types`, frontend typecheck/test/build. | Nuovi contratti API | risolto |
| DOMAIN-ART-001 | modello | Risolto: lo staging salva il blob soltanto come `Change.new_blob_id` proposto e non modifica `TrackGroup.art_blob_id`. L'applier legacy pubblica il current del gruppo solo se tutte le operation art staged sono accepted e applied con lo stesso blob; failure/partial/reject non lo cambiano e l'undo ripristina il valore precedente. | S2 | enrichment, persistence | Conservati BlobStore, refcount e applier; nessun secondo writer o aggiornamento anticipato. | `tests/pipeline/test_enrichment.py`, `tests/jobs/handlers/test_enrich_art_handler.py`, `tests/changes/test_apply_embed_art.py` (stage→apply→undo). | PRODUCT-REVIEW-001; data migration | risolto |
| BUG-APPLY-001 | bug API | Risolto: il service rifiuta prima dell'enqueue un ChangeSet draft senza change accepted; l'applier ripete il controllo prima di passare ad `applying`, coprendo la race con decisioni cambiate dopo l'enqueue. Lo stato resta `draft` e nessun file viene toccato. La nuova foundation applica lo stesso vincolo prima di creare `ApplyRun`. | S2 | changes/apply API | L'applier esistente è rimasto invariato nel percorso di scrittura; sono stati aggiunti soltanto i guard ai boundary. | `tests/api/test_changesets.py::test_apply_rejects_changeset_with_zero_accepted_operations`, `tests/changes/test_apply_undo.py::test_rejected_change_cannot_be_applied_or_touch_disk`, `tests/services/test_reviews.py`. | DOMAIN-CHANGES-001 | risolto |
| TEST-MIGRATION-001 | test/tooling | `alembic check` non è oggi un gate utilizzabile: interpreta le tabelle virtuali/shadow FTS5 come rimozioni e segnala drift preesistente tra unique index SQLite e `UniqueConstraint` dei model. Il report non contiene drift delle tabelle 0011, ma termina comunque non-zero. | S3 | Alembic, DB | Filtrare gli oggetti FTS gestiti manualmente e allineare la rappresentazione dei vincoli unique legacy senza cambiare lo schema effettivo. | `alembic check` su DB migrato deve risultare pulito e continuare a rilevare drift reale. | TEST-CONTRACT-001; migrazioni legacy | confermato (adiacente, non blocca Slice 4 Sol) |
| TEST-CONTRACT-001 | test | Slice 5–6 coprono matching e ricerca manuale/URL. Slice 8 copre fixture audio temporanee, drift SourceSnapshot anche dopo retry fallito senza side effect, scrittura combinata, collisione creata dopo il precheck, stat/hash catalogo, partial+retry, recovery che non dichiara falsi revert, undo bundle di lyrics preesistenti dopo move e cancel tra file. | S2 | test infrastructure | Conservati smoke Docker/Compose, fixture audio copiate sotto `tmp_path` e failure injection deterministica; nessun test provider live o accesso alla libreria reale. | `tests/changes/test_apply_review_bundle.py`, `tests/changes/test_apply_journal_recovery.py`, `tests/changes/test_apply_write_lyrics.py`, `tests/jobs/handlers/test_apply_handler.py`, `tests/services/test_review_apply.py`. | Tutte le slice core | risolto fino alla Slice 8; restano i contract delle slice successive |
| ARCH-LAYERS-001 | architettura/tooling | Risolto: il composer, il repository delle review e la composizione dei path sono nel layer `pipeline`; gli handler match/cover/lyrics/ReplayGain non importano più `services`. I re-export in `services` preservano i consumer API, manual-search e il compatibility helper paths. 7A–7C e l'apply path non sono stati modificati. | S2 | jobs, proposal composer | Contratto job → lower layers ristabilito, senza ricreare producer ChangeSet. | `uv run lint-imports`; `tests/jobs/handlers/test_match_handler.py`, `test_enrich_art_handler.py`, `test_enrich_lyrics_handler.py`, `test_enrich_replaygain_handler.py`, `test_enrich_art_review_handler.py`, `tests/services/test_proposals.py`, `tests/services/test_paths.py::test_build_group_resolver_does_not_crash_past_sqlite_variable_limit`. | PRODUCT-PIPELINE-001 | risolto |

## Ordine di chiusura

1. `BUG-REPLAYGAIN-001`, readiness/capability e test container.
2. `BUG-JOBS-001`, `BUG-LYRICS-001`, `OPS-SETTINGS-001` e stato provider.
3. `DOMAIN-CHANGES-001`, `BUG-CHANGES-001`, `BUG-APPLY-001`,
   `CONTRACT-API-001`.
4. `BUG-MATCHING-001`, `BUG-REVIEW-002`, `UI-CANDIDATES-001`, ricerca manuale/URL.
5. ReviewBundle completo con rename/art/lyrics/ReplayGain e operazioni file.
6. Shell, Inbox, Review, Catalog e rimozione dell'IA Groups/Bulk edit.
7. Reset, retention UX, hardening finale e acceptance Docker/E2E.
